
spring:
  application:
    name: JournalApp

  # Database Configuration (MySQL)
  datasource:
    url: jdbc:mysql://localhost:3306/JournalApp?useSSL=false&serverTimezone=UTC
    username: root
    password: 8858
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update




