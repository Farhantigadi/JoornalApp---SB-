<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Log folder and format -->
    <property name="LOG_DIR" value="logs"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender (per minute + size limit) -->
    <appender name="MINUTELY_ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>
            ${LOG_DIR}/application.log
        </file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Create a new file every minute -->
            <fileNamePattern>
                ${LOG_DIR}/application.%d{yyyy-MM-dd_HH-mm}.%i.log
            </fileNamePattern>

            <!-- Roll on size within each minute -->
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>

            <!-- Retention: keep last 60 minutes of logs -->
            <maxHistory>60</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="MINUTELY_ROLLING"/>
    </root>

    <!-- Your package-specific logging -->
    <logger name="com.EDigest.journalAPP" level="DEBUG"/>
</configuration>
